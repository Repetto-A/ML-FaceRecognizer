# ── API (VPS) ───────────────────────────────────────────────
API_KEY=cambiar-por-una-clave-larga-y-secreta
ALLOWED_ORIGINS=https://tu-frontend.vercel.app,http://localhost:3000
MAX_UPLOAD_MB=10
INDEX_DIR=./index
DEFAULT_THRESHOLD=0.65
TORCH_DEVICE=cpu

# ── Indexer (local) ─────────────────────────────────────────
TORCH_DEVICE_LOCAL=cuda
VPS_HOST=ubuntu@149.56.129.7
VPS_INDEX_PATH=/home/ubuntu/face-api/index
SSH_KEY=C:\Users\conta\.ssh\droplet

# ── Frontend (local dev — ver frontend/.env.local) ───────────
# API_URL=http://149.56.129.7:8100
# API_KEY=<la clave del VPS .env>

# ── Supabase (fase 3, opcional) ─────────────────────────────
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
